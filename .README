FIX Protocol Implementation

This project implements a simplified version of the FIX protocol
based on FIX 4.2 specification.

Only the essential tags required for the trading simulation are implemented:
- System tags: SenderCompID(49), TargetCompID(56), MsgType(35), CheckSum(10)
- Order tags: Symbol(55), Side(54), OrderQty(38), Price(44)
- Execution tags: OrdStatus(39), Text(58)

Message format: tag=value|tag=value|...|checksum
Delimiter: | (pipe character)
Checksum: Sum of ASCII values % 256, formatted as 3 digits

Reference: FIX Protocol 4.2 (simplified)

https://www.onixs.biz/fix-dictionary/4.2/index.html
